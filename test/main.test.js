"use strict";
var expect = require("chai").expect;
var main = require("../main");

describe("fizzbuzz", () => {
    it("should return ....", () => {
        expect(main.fizzbuzz(2)).to.equal([]);
    });
});