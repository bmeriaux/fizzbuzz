"use strict";
const expect = require("chai").expect;
const main = require("../main");

describe("fizzbuzz", () => {
    it("should return ....", () => {
        expect(main.fizzbuzz(2)).to.equal([]);
    });
});